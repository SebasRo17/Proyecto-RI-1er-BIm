calculating eigenvlaues negative positive define index nn mm well np mp matrix element function assign positive zero negative integer value nn np strictly positive integer mm mp code much complex simplified version look like nn mm som nn mm rx po n floor mm floor nn mm nn mod mm np b floor b mm floor b nn mm nn mp b mod b mm vnozer nn np mm mp nn np mm mp exp kx vzer nn np mm mp nn np mm mp exp kx v nn np mm mp ab nn ab np vnozer nn np mm mp vzer nn np mm mp en nn sign nn sqrt ab nn c b en n kroneckerdelta ab n ab np b kroneckerdelta mp b kroneckerdelta sign n sign n b v n np b mp b q array c som som mf table kx eigenvalue q kx rx rx rx po array ou som array pl som table ou table mf x mf x x po som table pl listplot ou plotrange som fpl table pl z z som show fpl eigenvalue plot look like would like get red plotmarkers eigenvalue positive say blue eigenvalue negative played around statement able figure right syntax something along line plotmarkers x red dotsize blue dotsize