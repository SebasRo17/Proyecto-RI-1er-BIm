wrap another common base base interface quite large involves writing lot pas function looking way avoid let make example car volvo implement every car interface call appropriate wrapped volvo except maybe something lower basically many function like obvious way solve would make subclass volvo car volvo seems violate principle favoring composition inheritance else could avoid writing wrapper language c position write use inheritance template magic could help