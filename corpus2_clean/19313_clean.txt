would like create data structure type header pointer pointer would point list shown example like work problem pointer unique storage storage far good want assign value storage evaluate pointer v v v v v v evaluate needed order enforce evaluation left hand side assignment pointer touched changed good trace pointer pointer storage v v v still point storage turn point list actual data yet start evaluating something beyond storage turn later main problem like something like header storage wrapper pas around function problem start one try header pointer header v v v pointer evaluates value thus get bust since pointer evaluates way much one try setattributes header holdall prevent evaluation one get ala evaluates little header pointer header pointer found way header tmp tmp trace pointer header storage finally trick really understand work actually trace pointer also evaluate value v v implementation might problematic several reason trace might come rather long list need second element another problem suppose output trace command might change dynamically depending whether one put extra definition suppose theoretically possible another way would issue header pointer command pointer still unspecified pointer unique storage object header pointer fix storage force something potentially might want example command object header come end whatever reason anyway feel construct look fragile think like understand controll thing better better way