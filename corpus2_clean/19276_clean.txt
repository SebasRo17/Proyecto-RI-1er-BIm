consider following code regionplot print x x plotpoints region private regionvar region private regionvar question regionplot create first two line output understand first sample point second sample point region private regionvar region private regionvar running mathematica recall happening earlier version edit changed code give better minimal working example leave code first gave along truncated output myfunc pt module bool bool sin pt print pt bool bool return true return false regionplot myfunc x x plotpoints false region private regionvar region private regionvar sin region private regionvar false true