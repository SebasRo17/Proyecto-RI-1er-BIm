project git branching model roughly follows nvie git flow release branch named semver format e g v1 num release branch given green light production close branch merging master applying tag deleting branch immediately delete release branch using identifier tagging branch e g v1 num command use close release branch git checkout master git merge v1 num git tag v1 num version num num foo bar baz etc git branch v1 num git branch dr origin v1 num git push origin v1 num git push git push tag seems work majority case however causing issue scenario another instance git repo e g another dev machine staging environment local checkout v1 num branch git push origin v1 num command delete branch remote delete local version branch exists repos lead ambiguous reference trying checkout v1 num repos git checkout v1 num warning v1 num ambiguous avoided without using different syntax branch e g release v1 num v1 num rc unavoidable therefore fundamentally bad idea create tag deleted branch