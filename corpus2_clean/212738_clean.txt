application working connecting virtual machine performed two way user either select machine list click connect button double click machine list currently two separate event handler slightly differently user already connected clicking button disconnect double clicking bring window back edit logic essentially button connected disconnect else list question keep method separate combine two method even though slightly different disconnecting reconnecting switch based sender