use c example apply globally say one constant also want client set use private foo private bar public foo get foo set foo bar public bar get bar use technique quite lot want know considered formal concept