working set rest apis need secured authenticated call performed multiple web apps service apis best practice approach authentication occur thought two possible place num web app perform authentication using shared authentication service seems tool like spring security configured web app level num protect web app gateway security approach web app never receives unauthenticated call seems approach apache http server authentication approach would use apache nginx protect something else apache nginx web app additional reference authentication similar service like aws non secret identifier combined shared secret key also considering using hmac also writing web service using spring update clarify request need authenticated identifier secret key similar aws rest request work