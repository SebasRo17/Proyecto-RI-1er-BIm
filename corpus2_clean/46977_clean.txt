trying use manipulate plotting multidimensional data higher dimension defined slider first simple example work myfun v real v real v real v real v real sin v co v v v v manipulate plot myfun v v v v v v v v v v v mathematica graphic dimension plotting control configured change dynamically mathematica get stuck manipulate myaxis sequence v ax v ax mycontrols sequence v sl v sl v sl manipulate plot myfun v v v v v myaxis mycontrols ax range controltype setter ax range ax controltype setter sl complement range ax ax controltype setter sl complement range ax ax sl controltype setter sl complement range ax ax sl sl controltype setter mathematica graphic make last piece code evaluate correctly