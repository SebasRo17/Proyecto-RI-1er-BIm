way usually make namespace platform platform h every o call encapsulated static namespace place entire base know o used platform cpp good way making thing easier instance call platform messagebox actually happens platform messagebox ifdef window elif linux elif mac endif