trying large amount site share num simply designed query api result common userbase database configured slightly different different cs perhaps even different templating initial idea separate application common library read site framework would allow single instance django may help reduce memory usage site framework right approach problem like real benefit running separate application initially thought think otherwise heard following site id set setting py order multiple site need multiple setting py configuration mean multiple distinct process instance course share base site need dedicated worker serve site effectively remove benefit running multiple site one hood site need uwsgi instance running alternative idea system know route taking