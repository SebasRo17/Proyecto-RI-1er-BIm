bother typedef ing number type like use place might important current project client server based type match transmitted wire binary wondered far people go generally type h typedef int16 version typedef int32 typedef int32 identity typedef int32 typedef int64 typedef int64 rfid typedef int64 typedef int64 typedef int16 typedef int16 typedef int64 typedef int16 typedef int16 typedef int16 typedef int16 typedef int16 typedef float typedef float typedef typedef typedef typedef typedef typedef percent typedef typedef typedef hour typedef minute typedef second typedef day typedef month typedef int16 year typedef seems logical try make sure always used particular thing avoid overflow often see used pretty much everywhere instead typedef ing often lead look bit like though dosomething make think bit actually describing parameter parameter parameter thanks