objective c concept nil would accept call parameter without complaining silently another nil used something similar using easymock mockito testing something similar even good idea something like one use case considering using optional metric metric counter call mark every time event generated surrounded check metric enabled flag could use nil counter silently accept mark call metric enabled