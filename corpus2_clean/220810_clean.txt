heard c program need link c library runtime however c runtime inserting stack operation image boilerplate c runtime basically intrinsic program linking runtime bit illogical linking library make sense especially since c library independent object would otherwise need source program relocation wondered besides c library else know linking exactly example supposedly linking c library also need link o specific library api well least wrapper sort deal directly linker effect link program file c library o api library sound particularly right basically linker link multiple library together statically example compile link time