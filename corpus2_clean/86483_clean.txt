work environment several different web application different feature still need similar thing authentication read common source store common etc better build shared functionality set service called web apps better make shared library webapps include service library would need access various database seems like keeping access single place service good idea would also reduce number database connection needed service would also keep logic single place could argued shared library thing benefit gained using service shared library