vms call stack keep track called often simply regarded stack however often stack needed operation related calling performing arithmetic question stack e call stack used also kind operation operation performing arithmetic use different stack talking vms