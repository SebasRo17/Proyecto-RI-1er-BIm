consider following cast public f1 b public f2 foo might b b inside block implicitly redeclare b b go f2 compile new enter block unless b therefore within block treat exactly declared b could assign block reference would compile time assign new example thanks bringing question anything could break assumption b could easily break following legal c well b b b b b f2 former look like syntactic sugar latter even feature make sense sure anders useful feature implement wondering worse idea think update serious objection would feature described add semantics without adding syntax one version language next meaning block would change significantly would tolerable new keyword adding new keyword