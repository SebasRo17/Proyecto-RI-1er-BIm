add new configuration option program often ton ripple effect term getting option need acted upon three basic way deal aware num pas configuration setting part program need explicitly primitive explicit way way decouples thing downside verbose brittle num make frequently used configuration setting global static simplest way introduces action distance hinders testability assumes configuration truly global want one configuration given time num make configuration struct contains configuration option whole program major concern within program pas around explicitly less explicit num explicit num want change setting one call clone config change one useful testing practice however still end potentially passing ton info need changing config struct still cause action distance would consider num pattern anti pattern anti pattern instead