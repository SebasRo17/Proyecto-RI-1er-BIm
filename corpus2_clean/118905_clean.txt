intrested know exists tool let example valid document let generalize reusable parser imagine everytime start learning parser get granularity something like lex yacc seems complicated instinct say left wondering fundamentally complicated need see b way build ad hoc dsl relatively simple task could learn update simple example ad hoc dsl might like make quickly instead xml foo item bar item item bat item foo might want something like foo bar bat line contain item indentation produce parent child relation imaginary tool let convey information imaginary tool might highlight foo right click point would prompt restrict value choice list might extend first one foo bar num bat surround sub item might qualified boolean specifies recursion set bar num might work kind train thought generally led ask question possible qualified answer change apologize advance