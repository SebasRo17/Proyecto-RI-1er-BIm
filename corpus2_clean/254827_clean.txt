working net application wondering use separate method handle click event two different button essentially thing different object form case two separate map private sub sender e eventargs handle click click sender else end end sub separate method private sub click sender e eventargs handle click end sub private sub click sender e eventargs handle click end sub overhead checking two object one version easier read something completely different