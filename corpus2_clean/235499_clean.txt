title say looking way store many dictionary one per user dictionary datetime enum database sql server num dictionary num item inside represents calendar day certain status preferably want store item separate record db idea update suggestion made also come another optimisation rather default creating new calendar represented dictionary item user add let pick predefined one drop work create separate calendar ability reuse calendar storing default value would deal lot less long term