let say procedure stuff dostuff discover stuff quite operation procedure becomes large split multiple smaller procedure soon realize kind state would useful stuff need pas less parameter small procedure factor private public start private helper procedure call like new start like new start feel wrong using net api always never call new start make suspect wrong sure could make constructor private add static helper public static dostuff new start whose external interface consists one static also feel weird hence question well established pattern class one entry point externally recognizable state e instance state required execution one entry point