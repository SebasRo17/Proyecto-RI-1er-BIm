realize idea might seem really foreign started thinking bit back far web site go least experience far time spent retyping thing way around instance say user model user userid wanted log user view controller database controller view point whole exchange need one typing need database kind expected wanted show list user based group id thing happens except userid conversion happens many list end represented ui understand might larger memory wise type bool example costly typing happens could productive needed validating number date ect leave string part