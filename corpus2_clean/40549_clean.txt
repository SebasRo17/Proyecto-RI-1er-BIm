boxwhiskerchart automatically us width box plot would like force use list width width corresponding box working time series data would like width box depend duration dataset collected regardless sampling rate example box containing data collected hour would twice wide data hour one way get variable width use method boxwidth box width scale according number point dataset data duration boxwhiskerchart data method boxwidth scaled since sampling rate variable width reflect duration measurement possible apply list duration boxwidth somehow many thanks