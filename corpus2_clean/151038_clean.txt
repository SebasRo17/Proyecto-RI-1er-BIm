application need store client part employer well assuming client one employer chance people identical employer slim none schema would make sense use schema num client table id varchar num email varchar num address varchar num city varchar num state num zip varchar num employer varchar num employer phone varchar num employer address varchar num employer city varchar num employer state num employer zip varchar num schema num client table id varchar num email varchar num address varchar num city varchar num state num zip varchar num employer table id varchar num phone varchar num address varchar num city varchar num state num zip varchar num patient id part think since clearly two different object real world seperating two different table make sense however since client always employer also seeing real benefit seperating would make querying client complex benefit reason creating two table situation like one instead one