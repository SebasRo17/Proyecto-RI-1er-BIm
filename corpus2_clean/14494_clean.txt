need attach column matrix join matrix make longer row operation use frequently find join function ideal case join matrix make longer row join attach column matrix join list join two column make matrix join list list however wanted define function would simplify notation needed reach goal columnattach list list join vectorq list vectorq list columnattach columnattach columnattach work expected would like generalize bit instance join take list vector matrix length join list list adapt columnattach function achieve flexibility