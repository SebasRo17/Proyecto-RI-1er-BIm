dabbling idea creating cm experience would fun website base one decision keep coming back best validate incoming parameter function mostly reference simple type since validation would quite bit complex first debated creating naming convention would contain information parameter bool etc also figured could create option validate every still need sort parameter validation parses parameter determine validate granted would handled passing list parameter still need happen one goal remove parameter validation actual accomplishes intended task without additional validation good way handling low level typically parameter validation done start call anyway stick