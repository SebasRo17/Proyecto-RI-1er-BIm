suppose language instead statement function could defined relation function operator partial application composition would characteristic language could everything done statement done design would limitation language like