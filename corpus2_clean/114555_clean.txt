language language feature handle case whenever condition becomes scope context extra could specified execute note different simple must particular scope explicitly written clause would almost like applied every case execution program