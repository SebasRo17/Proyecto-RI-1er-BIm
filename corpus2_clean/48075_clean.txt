puzzled model situation suppose algorithm operating every procedure p must take place whose role modify p reality different flavor p say p1 p2 p3 choice p user dependent p produce called well base apply specific p1 apply p2 apply p3 apply actual p get instantiated factory stay simple case p4 follows principle time need additional current iteration average previous iteration would redesign case