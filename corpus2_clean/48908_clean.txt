two list l l key data let u say key person name string data follows respond rasher query let u also assume list sorted key l joseph rourke data data would like align two list following sense l name l l padded include blank record vice versa l name b l l modified include blank record b two list align l c l b maybe list aligned fashion could make two column table one column per list would directly compare one list question clean method accepting l l input returning l l output latter two list aligned accomplish via tedious list loop suspect cognoscenti offer concise efficient method thanks help