structure trade performance access need iterate clearing keep generation counter entry also global generation counter clear operation increase generation counter access compare local v global generation counter differ treated clean come answer stack overflow recently remember trick official one use case dijkstra algorithm tiny subset node relaxed done repeatedly