want know dispatch actually rule list fast documentation say dispatch generates dispatch table us hash code specify set rule need actually scanned particular input expression hash table specify set rule need actually scanned particular input expression deletecases since fast use hash table