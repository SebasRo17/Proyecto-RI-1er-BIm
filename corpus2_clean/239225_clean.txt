fairly new c trying learn best practice faced many situation last week need make choice longer simpler shorter combine several action single statement standard veteran coder use trying write clear concise example writing two option preferable startswith analog point regex split last precision parse num else precision b startswith analog point precision parse regex split last num else precision c precision startswith parse regex split last num