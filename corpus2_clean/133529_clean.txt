suppose allows kind configuration well known default value also option defining custom example suppose want let user configure person title choose set standard value mr mr m etc also specify custom dr elegant way put api example public person public enum title mr mr m custom settitle title title one quite bad whenever specify anything else title custom second argument silently ignored maybe public person public enum title mr mr m custom settitle title title title really like one either immediately clear calling one overwrite effect elegant interface overlooking