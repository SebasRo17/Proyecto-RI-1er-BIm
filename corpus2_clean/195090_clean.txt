trying learn lisp looking lisp difference see implementation scheme use square bracket interchangeably round bracket readability treated assume still expression like everything else however clojure see square bracket curly brace used represent different type like vector also still valid expression must considered atom break whole concept expression thereby making clojure impure lisp