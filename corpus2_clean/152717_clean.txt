hierarchy extends c extends b extends example would top looking short concise way describing position within hierarchy specific deepest hierarchy last subclass final might might final sealed c matter last one hierarchy particular moment newer e appears extends proposed term would apply also might one fit description e extends c term apply class