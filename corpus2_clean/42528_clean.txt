unidimensional array let call list ranging one representing second day day sec example list enter image description goal change tick plotted list listplot instead number give hour also must tick one representing one hour example number would become