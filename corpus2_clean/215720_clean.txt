implemented builder pattern domain help understandability actually passed constructor normal advantage builder give one twist exposed builder interface could chain required function function make sure correct parameter passed curious existing pattern like example public foo private something private something2 private datetime something3 private foo builder builder something builder something something2 builder something2 something3 builder something3 public static new builder public interface public public interface public datetime public interface public public foo build public static builder implement private something private something2 private datetime something3 public something public something public something public foo build new foo example called foo foo foo num datetime build foo foo2 foo num datetime num build