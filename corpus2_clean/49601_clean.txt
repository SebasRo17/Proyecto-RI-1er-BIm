sure either new repo clone repo workflow setup correctly create new project create repo github clone empty create new project go workspace git init run workspace copy end everything workspace project however clone github first need clone repo go default git directory c git git cloned project need import git repo project workspace end workspace cloned project effectively duplicating project folder git area tried clone workspace cloned project asks import git project try use workspace cloned project errs wrong thanks matt