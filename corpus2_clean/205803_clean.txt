recently read question feature arrow anti pattern something similar trying refactor except branch look little something like else else else else short look like something endif else something endif endif else something endif endif endif outline borrowed coding horror article subject go different permutation various flag flag set somewhere elsewhere either user result make kind readable intention eventually bean contains choice previous programmer tried capture branching anti pattern instance outline really three enum set inside bean enum option one option two option three boolean choice boolean choice boolean choice acceptable cure better one