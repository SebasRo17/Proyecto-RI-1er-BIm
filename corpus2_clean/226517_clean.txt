starting learn lisp using sicp book author mention procedure e recursive iterative additionally process procedure generate also recursive iterative surprisingly recursive procedure sometimes generate iterative process example given factorial procedure recursive procedure generates iterative process define factorial n iter num num n define iter product counter max count counter max count product iter counter product counter num max count quotation book one reason distinction process procedure may confusing implementation common language including ada pascal c designed way interpretation recursive procedure consumes amount memory grows number procedure call even process described principle iterative implementation scheme shall consider share defect execute iterative process constant space even iterative process described recursive procedure question understood principle involved e still understand lisp interpreter generate iterative process recursive able calculate using constant space language able