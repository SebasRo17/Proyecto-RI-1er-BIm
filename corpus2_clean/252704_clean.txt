making io app contains museum gallery user look exhibit select info etc exhibit screen two different fed common source view set image two top grey square preview bottom one see picture view lazy loaded question need transform preview image proper size go somehow pre process make loading faster use pre processed resized image preview benefit lazy initialisation lower waste resource slower loading compared ready image side preprocessed image waste storage network image loaded stored locally would better performance point view taking account app smoothly iphone num oldest io num good idea combine two method cache lazy loaded image