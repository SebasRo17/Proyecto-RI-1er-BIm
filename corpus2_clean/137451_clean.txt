writing tool cli unix make program help usage usually use fprintf stderr help text several issue first sure whether use stderr ok use stdout imagine help text quite long depending many option tool usually put several string like n second parameter however fill source fifty line help text easy manageable instead tool written c c like language tend use doc possible prominently perl use c something like could use considering putting h inside define help help text never seen wild know whether actually use ideally could put text external file include using include seems wrong though idea help text easily manageable inside source really convenient