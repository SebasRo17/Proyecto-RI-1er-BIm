rule num rule num new c standard state need explicitly declare either destructor copy constructor copy assignment operator probably need explicitly declare three hand martin clean advises remove empty constructor destructors page num clutter use default constructor implementation serf clutter meaningless artifact handle two opposite opinion empty constructor destructors really implemented next example demonstrates exactly mean include iostream include memory struct const v new const v new v operator const v reset new v std auto ptr v const num std cout v std endl b std cout b b v std endl const c num std cout c c v std endl b c std cout b new b v std endl compiles fine using g num num g std c 0x wall wextra pedantic example cpp destructor struct empty really needed removed