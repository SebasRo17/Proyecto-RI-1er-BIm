working c suppose boxfilter used filter box property height width depth weight etc filter might something like box width greater would pas filter usage would something like c style pseudocode filter new boxfilter foreach box box box filter passesfilter box something sorry c example think easier understand setting filter need configure setting filter filter currently num property although could get time debating whether filter multiple setter etc create struct single box filter called setting