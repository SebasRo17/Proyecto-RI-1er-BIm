let say namespace namespace contains foo prefer nesting typedefs hold namespace namespace foo stuff nested one typedef bar foo bar namespaced one typedef bar foo bar looking std libs thing namespace variant maybe bad example istream nested iostream point mean way using std fstream std istream use directly also list possible function quite huge std could first variant encapsulated also much inheritance lot foo use using namespace first thought nested variant nice encapsulation want group also could new namespace inside also every time new namespace also without using namespace