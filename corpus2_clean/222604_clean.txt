seems often encountered practice wondering design pattern following suppose represents card public hand private static random rng private rank rank private suit suit public hand prng prng new random stuff every time instantiate card want random card way waste one comparison per instantiation create static field allocate deceleration private static random rng new random new random get instantiated every time also efficient pas random constructor violate tell ask perhaps another suggestion far think best way check