working array data may look something like data want replace sequence one exceeds two row number zero order maintain original dimension array came following try data x patternsequence repeated x join table join unfortunately replaces first two one sequence zero leaf rest exactly aiming