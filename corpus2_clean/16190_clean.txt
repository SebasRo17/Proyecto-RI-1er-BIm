question topic could get answer reading want use apply argument function h map another argument attempted map apply h b h b h b element want map never get used want want h b h b could use table instead map slow slower using apply twice hoping map would faster understand apply using sure syntax correct force first slot used map instead apply edit like actually want map apply h h randomreal randomreal want output h b h b h b h b b random number get think order apply map important