software car model used different view listing ad detail page sell widget etc extends dbmodel look nice way combination flexible easy maintain etc used view thinking two different way num view context var car constructor internal car based gettitle extends extends busines rule change ad widget num extending directly challenge come car model might need abstract factory let say field car state car truck bike van would affect view let say gettitle rule would different would