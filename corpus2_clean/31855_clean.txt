set pixilated shape transforming shape morphological component want able return coordinate pixel lie contour shape specifically want pixel number background neighbor von neumann moore neighborhood say shape phrase like following example png isolate color morphological component pixilated shape care follows shape import morphologicalcomponents colornegate imagecrop shape colorize return von neumann moore neighborhood connected set contour pixel component