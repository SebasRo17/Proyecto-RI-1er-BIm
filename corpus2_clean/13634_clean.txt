want able high dimensional integral like naively wrote nc nn f x sin x g n x co n x integrate time flatten table f j nc j nc seriescoefficient series exp plus flatten table n x n x n plus flatten table g n j nc j nc n nn x pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi pi like running age without output would like know optimize work push higher level instance would like extract higher power x rather example also variable