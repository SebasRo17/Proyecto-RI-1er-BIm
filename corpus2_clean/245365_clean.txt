might related yeoman project might adopted somewhere else seems default generator aside yeoman generator also noticed project nothing yeoman one example convention come reason using src folder example used common random preference p far tell seem strictly related web app instead web site rather subjective anyway