switching c recommendation codereview looking lwjgl one thing remembered every call display must executed thread display create invoked remembering whipped look bit like public implement private final static num private final scheduler scheduler public scheduler displaymode displaymode throw lwjglexception scheduler display setdisplaymode displaymode display create public dispose display destroy override public override public future boolean scheduler schedule display iscloserequested writing notice created called return future boolean dispatch scheduler interface nothing wrapper around writing schedule scheduler noticed could either use supplier argument callable argument represent passed contain override supplier noticed lambda expression display iscloserequested actually compatible callable bool supplier bool question difference two semantically otherwise adhere