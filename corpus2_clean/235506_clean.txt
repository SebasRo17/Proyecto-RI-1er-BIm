basically would like obtain way iterate one list call method specific interface object collection implement project would result something like would empty abstract hashset component new hashset component add instance extends implement drawable updatable drawable component draw updatable u component u update friend suggested make two separate list mean object list also think make sense abstraction wise one list iterate one list also like empty inherit question approach make sense elegant pattern could follow