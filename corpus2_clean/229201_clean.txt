old handle printing spiral need help developing algorithm approximate spiral lisp sub script written us varying radius draw spiral setq inner radius 1e3 spiral grows inner radius outer radius setq thickness num half pitch spiral pitch num setq outer radius num num thickness inner radius outer radius setq p1 list num inner radius setq p2 list inner radius num setq p3 list num num inner radius setq p4 list inner radius thickness thickness setq p5 list num inner radius thickness num command arc p1 p2 p3 draw num point arc p1 p2 p3 command arc p3 p4 p5 draw num point arc p3 p4 p5 setq inner radius inner radius thickness num using autocad implement script cop implement spiral autocad plot dated able handle spiral handle circle arc segment seeking implement better approximation spiral instance tried thinking instead used sub script come viable scheme