multiple regexes matching greedy first mode regexes already compiled see speed thing mean using tool coming net framework since replace capturing wonder make sense built regex matching result would execute matching per multi regex instead multi matching per single regex thinking far way speed