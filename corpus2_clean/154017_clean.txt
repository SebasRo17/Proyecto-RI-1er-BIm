creating ruby game using framework good sometimes game kind slow startup probably rather slow whole game close open likely startup quickly whole game smoothly fast phenomenon faster cache stored whatever since first would cache stored app dy would expect reference etc ruby window num