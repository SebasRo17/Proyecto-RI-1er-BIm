list upper triangular would like interpolate cannot unless set interpolation order tried making list rectangular filling bottom half null allow interpolate higher order however interpolation function end early return value distance boundary diagonal element list relatively small would like full interpolation close boundary advice list would like interpolate code using temp constantarray null table temp ixa ixb data ixa ixb ixa range ixb ixa f listinterpolation temp