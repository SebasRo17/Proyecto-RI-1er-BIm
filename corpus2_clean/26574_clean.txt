large set ordered pair trying add second element together group pair first element example data look like data want return something like newdata figured group data based first element using gatherby data first sure go feel like probably much easier way evading