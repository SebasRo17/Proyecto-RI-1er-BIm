writing application graduation coursework c using gtk ui library sometimes ask user need save later use doubt come store want create global variable told countless time use however need interchange function call directly slot signal pattern always accessible event start thus needing independent storage purpose designed getter like read similar store clear static stored clear stored else stored else stored construct ok simply use global instead