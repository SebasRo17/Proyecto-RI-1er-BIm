talking normal pc application memory sufficient declare global member variable use global member variable beginning variable initialized old never used keep variable chance reuse old list car public list car always init new list car fetch db public list car n id always init new list car fetch db