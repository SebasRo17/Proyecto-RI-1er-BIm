read equivalence partitioning used typically interval list e g assume used every set input anyway requirement say allowed color red blue black green cannot treat like list right mean testing one would enough developer likely used switch case thus real set one could represent also others meant list also clear think always possible initial design test case checking two line intersection mx c two input num line m1 m1 c1 must different c2 num line intersecting m1 must different m2 num use partitioning actually taken book say set eq class