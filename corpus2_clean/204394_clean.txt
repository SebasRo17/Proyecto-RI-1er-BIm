often table natural key still useful user able uniquely generated identifier table surrogate primary key case would certainly expect key exposed user another field used purpose one reason expose surrogate key operation preserve relationship record change key value certain kind deletion insertion many method copying one database another etc advantage exposing surrogate key simplicity using field anyway circumstance better directly expose surrogate key user