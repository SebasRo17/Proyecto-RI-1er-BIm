trying declare initialize struct using pointer compiling fails anything thing thing num num num text linux give core dump try thing num detail text work separate assignment thing num detail text text include stdio h include stdlib h include h text abc typedef struct counter union number text detail thing thing one thing two one counter num one detail number num two counter num two detail text malloc num sizeof strcpy two detail text text thing thing num num num text thing num detail text text printf n n one counter one detail number two counter two detail text printf n n thing num counter thing num detail number thing num counter thing num detail text num help would greatly appreciated