often fight whether put certain key web config constant c something like example wanted store application specific key whatever case may could store grab web config via custom key consume referencing constant constant would want use constant config key question really applies language think