mutable local variable used internally e g side effect least intentionally still considered non functional e g functional programming scala course style check considers var usage bad question side effect writing imperative style still discouraged e g instead using tail recursion accumulator pattern wrong local creating local mutable adding long changed answer yes always discouraged even side effect reason