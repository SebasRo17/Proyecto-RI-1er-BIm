currently working set report many different section requiring different formatting trying figure best way structure similar report done past end large num function manipulation formatting report workflow look something like datatable new datatable dataclass would like able break large function smaller chunk afraid end dozen non reusable function similar everything whose job call smaller function like go one step really better solution organizational point view suppose e everything much organized might otherwise far readability sure potentially large chain function call function thought