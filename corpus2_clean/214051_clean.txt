currently font stack font family courier new courier monospace browser test see font displayed courier new font except android device nexus kindle although monospace fallback would rather site displayed across device violate license add font using font face resolve font face font family courier new src url font ttf format