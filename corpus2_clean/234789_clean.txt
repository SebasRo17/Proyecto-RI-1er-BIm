container containing object item different class especially common base e g container container container public icollection item item get item public container container get like enforce item need exactly one container like make sure never inconsistent situation e g container item contains return could done e g adding container additem set item container property accordingly item take care container item example item private container container public container container get container set container container item remove container item add could easily done concept friend using c friend thus example compile however course would compile container item public setter would allow inconsistency user class would need know use setter container item item container instead searching quite time feel way enforce restriction seems trivial using internal might work around solution elegant way enforce one container per item rule without concept friend