list like list want color red dont want count element like solution seems awefully complicated table list x style list x red list x x length list else could without using table word simpler way possible