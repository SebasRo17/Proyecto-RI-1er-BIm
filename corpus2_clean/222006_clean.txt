object created entered database guarantee field unique except auto incrementing primary key would like able access object later program using primary key best way get key database think two method obtain primary key adding database num find maximum primary key one recently added num delete current compare database program create new based row contained program seem prone hacky wondering standard way