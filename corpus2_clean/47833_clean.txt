neat way define function single optional argument default empty sequence example suppose want define wrapper randomreal allows zero one argument myrandom randomreal myrandom x randomreal x see two compact way first allow number argument check whether zero one myrandom x randomreal x length x use default argument sequence wrapped hold myrandom x hold sequence releasehold randomreal x neither seems neat might problematic inefficient realistic case conventional neat way sort thing