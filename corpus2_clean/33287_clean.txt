use listd table findroot eqns depending k k conc get list solution different value conc listd form v v v v eef v v v v eef v v v v eef v v v v eef v v v v eef v v v v eef tried export listd export file txt transpose prepend transpose listd conc csv give principle right output contains etc option export without instead use first row label output e g conc