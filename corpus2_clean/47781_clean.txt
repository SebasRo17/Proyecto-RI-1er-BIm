convention use distribute source c library built plan use cmake build system rest wondering standard term file folder subfolder naming inclusion path etc got far put everything prefix folder prefix bin prefix include libname prefix lib use cap libname internal includes contain libname folder e g say two file project principal h secondary h principal h inclusion statement secondary include secondary h include libname secondary h standard followed