using mathematica german window bit trying export import german text pdf import seems confusion character encoding obviously differs export import however nothing related encoding described pdf documentation specifying characterencoding pdf import export seem make difference importstring exportstring string aber ist mit und also x b und b measured pdf wonder work around require complete mapping pdf export slow character character probably take hour