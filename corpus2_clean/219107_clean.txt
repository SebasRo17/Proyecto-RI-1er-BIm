dictionary object column dictionary double column column column e post etc mobile phone would like make column much open extension possible trouble add class need set column always would like user able define column display remaining also add custom column column getvalue idea create abstract column derive specific column make child implement getvalue making abstract force overridden correct define enum list available column type finally column would keep available column dictionary dictionary enum column question thinking process correct room improvement would better idea use interface finally design going efficient fast rapidly changing value column e within second might update num different record dictionary