imagine always button labeled continue position app gui would rather make single button instance take different action depending current state private state currentstate state step1 private click switch currentstate case state step1 dothis currentstate state step2 break case state step2 break would rather something like public form visible private click dothis visible visible private click