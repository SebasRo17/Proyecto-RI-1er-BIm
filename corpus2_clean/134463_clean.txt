new html5 feature made recent browser allow sharing state among tab browser window domain window etc probably gotten little easier write web application keep lot state client also manages state intelligently tab window currently library like backbone j help manage state single tab single window anything yet help multiple tab window managing global state tab specific state cross tab eventing without relying server sync