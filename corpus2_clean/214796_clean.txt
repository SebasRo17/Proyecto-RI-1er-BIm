trying refactor display separate implementation sure put existing enum project currently structured follows utility depends utility depends utility depends utility depends utility trying add depends utility list adding new dependency contain set interface two project implement use populate view consume result enum currently used project except utility thus life project everything else depends new depend either project know enum option see create new project shared enum value add utility even though related define second time require convert one access shared view add dependency even though supposed independent source better option structure flawed begin