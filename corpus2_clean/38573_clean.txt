would like plot function family following set function table n x n work wonderfully plot table n x n x plotrange try generalize thing go wrong plot table n numerator denominator x n x plotrange plot expected reason give would expect see table n numerator denominator x decided see problem table table n numerator denominator x n plot x plotrange well give expect seems reason using numerator denominator function cause bug plotting somehow year learned often bug really disconnect understanding thing really work confused following produce alternating red blue line show plot table n x n x plotstyle blue plot table n numerator denominator x n x plotstyle red plotrange plot missing something would different table n x n table n numerator denominator x n show plot x plotstyle blue plot x plotstyle red plotrange