working ui action something like public myaction extends action public myaction settext action text action tool tip setimage image action created pretty much assumed action customizable sense text tooltip image changed anywhere late need changing action text location suggested co worker remove hardcoded action text constructor accept argument everybody forced pas action text something like public myaction extends action public myaction settext action tool tip setimage image however think since settext belongs base flexibly used pas action text wherever action instance created way need change existing myaction would look something like myaction action new myaction creates action instance hardcoded text action settext user required new action text overwrite existing text sure correct way deal problem think mentioned case user anyway going change text force constructing action benefit see original user create action without much thinking setting text