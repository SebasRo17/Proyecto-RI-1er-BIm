writing server backed pre written api going get number string representing port signal path etc etc etc need look associated given label object memory sql magic question best associate given unique label mutable represents enough object looking every signal every port find one match possible may slightly slow honest direct look every probably good enough small body object anything else premature optimization still curious proper solution would thought signal going grow bit larger see two option available first would create store simple map label could every time call addobject automatically saved hashmap like work relies properly adding deleting map grow indefinitely biggest issue involves hidden static map modelobject feel wrong somehow option interpret label label derived underlying object look signal label instance say num character port figure port need would allow quickly figure need however label changed need updated well everything break solution cleaner possibly cleaner solution either record working sufficient number variable make direct comparison little slow enough concerned memory overhead written object label need look extend parent