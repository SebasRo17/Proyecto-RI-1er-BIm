webapp war includes source using webserver accepts web project source uploaded docroot folder docroot admin webapp zip would place upload webapp handle way compiled binary source together road even compiled might compiled first runtime common file type webapp source mean lining standard would glad use similar extension zip directory structure probably use something like web zip would use zip webserver would know serve zip use webapp