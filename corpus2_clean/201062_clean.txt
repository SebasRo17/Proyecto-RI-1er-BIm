seem see often enough others nothing seems horribly wrong annoys look like done better suppose case statement might make little sense often work well case statement depending language flag else else b flag else else c flag else seems multiple way factor num set elses one set handle flag good way factor perhaps else algorithm happens usually mean something wrong