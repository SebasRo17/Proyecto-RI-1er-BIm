programming language like ruby use symbol understand manipulation much slower using lookup table well idea string memory matter different one used previously interpreter compensate would seem like interpreter still parse word typed order match symbol instance take mykey treat mykey behind scene anyways even key dynamic interpreter know key going find still treat key symbol eg key come interpreter still treat mykey know find key