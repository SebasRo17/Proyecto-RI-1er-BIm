question type mathematica assume every symbol mathematica type appears head expression implement operator monad associated symbol let f instance monad lift function return haskell notation return p f p monad bind function haskell notation rule bind f p f f f p monad maybe example point view category theory mathematica one define common rule symbol model monad multiplication flatten repeating head monadmultiplyrule p p params p params expression f f f p monadmultiplyrule f p also monad axiom satisfied return act neutral element bind bind return p f f f p bind f p identity f p sequential bind two function f q single bind composition bind f f p q bind bind f p f q bind f f p q produce result f q f p mathematica provide natural maybe like monad every symbol provide natural framework explicit monadic computation