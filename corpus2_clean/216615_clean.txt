question might suited math stackexchange com go version reconciliation take two part first exchange exchange update first paraphrase paper description step exchange two peer send one another set pair peer max version peer network one responds set delta delta look like peer key version tuples peer state map key given version version number greater maximum version number peer seen seems require node remember state node highest version number id node seen question must iterate peer exchange information p q