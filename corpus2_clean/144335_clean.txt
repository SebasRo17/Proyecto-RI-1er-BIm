working backend web application document repository fedora common specifically house xml file want abstract api repository internally tightly coupled one product also like give flexibility connecting repository via rmi rest apis hoping get advice resource implement something like thought abstract repository method like updaterecord deleterecord constructor would pas uri repository api port would allow flexibility future rest api became practical allow flexibility using rmi could better performance thinking right path