using view class templating view class often require routing get uri page could actually say routing available every view wondering would deal dependency injection injecting constructor would mean include every view many controller would seem wrong would also include trough extending think view deserve base routing library would able break solid rule dependency directly related child last option would injecting trough public injects single place template rendering guy solve problem looking clean non repetitive way