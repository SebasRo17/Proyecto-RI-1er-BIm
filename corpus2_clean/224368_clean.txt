process integrating unit testing framework existing base c zeroed turn use mocking framework like conjunction reading tutorial mocking infamous turtle example general idea define mock using macro etc declare mock pas unit testing many occurences existing base possible pseudo example clarify public bool foo obj b obj bar num b else simplified also exception primitive type may also object etc get general idea also creation object always direct may sometimes use factory want write test foo internally creates need mock bar return different value different path foo traversed unit testing nutshell problem tested internally creates need mocked hence passing instance mocked possible use mocking case specific mocking framework make possible