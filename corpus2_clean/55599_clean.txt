want demonstrate multiplication partitioned matrix example using insert menu build matrix draw line row column however want able select subset column row line add would like able compute product two compatible matrix column partition left matrix match row partition right matrix output show partitioned matrix row divider come row divider left matrix column divider come column divider right matrix example matrix multiplication defined since column partition say row partition b resulting matrix c row divider nd row since row divider second row b column divider