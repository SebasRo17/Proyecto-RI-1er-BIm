possible cause notebook hidden visible false instead closing pressing close button title bar message window seems work way notebook function list even visible closed special behaviour possible make notebook window behave like motivation part user interface would like gradually collect information notebook let call log notebook visibility log notebook would toggled button log notebook persist across kernel session across front end session never saved