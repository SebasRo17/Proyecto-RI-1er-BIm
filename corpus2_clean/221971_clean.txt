case joining table inside subquery outside subquery give difference count case num num row case table c joined inside subquery joined action table selecting filtering whole query table case num num row case table joined outside subquery joining key self explanatory mysql query select count select b c column table table mention left join table b b col b case num left joined matter joined nothing left join table c c c col case num statement c table participating group ing clause group order case num table c joined outside subquery left join table c c c col case num statement table c still participating course also know mysql left join work found second table row corresponding field table set actually happens figure also case num faster case num also sure joining case num contains least double amount row case num first filtered joined thought faster way