teaching structure difficulty coming good example use teaching queue two major application use multithreaded message passing mt programming scope course bfs style algorithm covering graph later term also want avoid contrived example thing think actually going solve single threaded fashion would use list rather queue tend use queue processing discovery interleaved e g search special case like length limited buffer e g maintaining last n item extent practical trying teach student good way actually thing real program toy show feature suggestion good simple algorithm application queue use example require minimum prior knowledge