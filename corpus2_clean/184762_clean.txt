undergoing technical interview based c skill would company allow expect use stl container solve problem think clear question pertaining specifically linked list knowledge linked list probably able use stl list container hand could get question using std vector might really useful question concern std vector directly latter case would think using vector stl container would acceptable course answer question least somewhat company dependent curious bigger tech company approach situation