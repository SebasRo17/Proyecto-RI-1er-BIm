developing model marketing database import transaction customer inventory etc file directive one process work every client told every client different import layout different column identify table primary key initial idea get definition client column make record unique store mapping table lookup table translate primary key internal surrogate key automatically every destination table every table conforms integer primary key matter many column type used make real pk first major problem saw map back lookup table get store model ben assured anything ever want query duplicated lookup table concern kind flexibility seems like cause serious limitation num technology stack way dynamically map import file ssis need lot dynamic sql c num scalability based previous concern initial testing would difficult scale without speed concern num complexity already running complex change try implement table historical change logging maintaining mapping lookup table example question feasible another obvious solution missing