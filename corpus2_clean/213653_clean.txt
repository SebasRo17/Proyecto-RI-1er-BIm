as3 got awkward rule scope due use hoisting like leaving bunch warning lying around important readable properly written regardless produce couple duplicate definition warning private dimension horizontal var uint num target var target x x num else var uint num target var target x num generally considered better conventional remove redundant var syntax second proper leave due way usage isolated two loop thanks