created app manages workflow row imported need store arbitrary number key pair addition standard mysql schema key value need searchable using rail mysql currently option adding new field rail schema new requirement arise need way store extra row might apply row