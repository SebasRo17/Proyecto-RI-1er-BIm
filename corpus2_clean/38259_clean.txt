want solve system simple differential equation point need solve non linear system algebraic equation newton method would like efficiently create right hand side vector using table similar command problem system consists two equation need f table something k fact f k entry example k length f return achieve efficiently know create different value odd index even need additional break whole code two table execution would like avoid could possibly also create two element list b flatten end still seems quite expensive