need find position first element list either greater zero criterion symbol something like lst k result position symbol following command would job first flatten position lst ab add matchq symbol answer always zero many thanks