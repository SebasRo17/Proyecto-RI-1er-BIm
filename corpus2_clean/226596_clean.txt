code table store pair group example let demonstrate two row row would represent one drop group drop 1 num group drop 1 num num would apart drop webpage selected submitted backend would get drop selected depending drop selection want something need compare request command something want something like requestparam equal something else requestparam equal num something else query database get appropriate code back check equality would know action perform