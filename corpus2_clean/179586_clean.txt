thinking applying real dependency injection library toy c gtk desktop application chose ninject think irrelevant question database window several utility window class clear inject database every window di useful make sense inject utility window class window class example class mainwindow utility class opened mainwindow utility window also opened utility window generally might really complex graph open class might need quite lot window class injected worried usage go suggestion inject many class become smell use kind service locator