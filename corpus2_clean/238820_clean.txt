recently came across construct never seen wondering whether use seems called initializer block public test public test first constructor public test second constructor dostuff block copied constructor e multiple constructor rewrite however see three drawback using syntax num one case order important define multiple block executed order written seems harmful simply changing order block actually change num really see benefit using case constructor call pre defined value even case could simply put private called constructor num reduces readability could put block end constructor normally beginning quite counter intuitive look completely different part file expect necessary statement language construct introduced legitimate use case