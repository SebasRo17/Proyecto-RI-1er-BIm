want use table generate list allows one iterator increment time table r r r run change run r etc giving element x array want list element r changed time parallel would give etc giving output want possible table anything else also know could table example project way work also possible would like use list one iterator value important