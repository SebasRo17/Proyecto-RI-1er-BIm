c program prog ported would called port c would prog port would called name quite obvious c port would called strike strange program ported visual basic c often called iron example ironpython ironscheme ironruby etc