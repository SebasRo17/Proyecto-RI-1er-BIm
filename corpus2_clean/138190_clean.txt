javadoc listiterator say listiterator current element cursor position always lie element would returned call previous element would returned call next listiterator implemented point element rather current element seems make client less readable repeatedly call getnext getprevious assume must good reason choice side note wrote little library called arraylist extends arraylist iterator listiterator provides method implemented like override public synchronized next previous