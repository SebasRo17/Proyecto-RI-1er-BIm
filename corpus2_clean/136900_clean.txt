good practice call return value statement something like private verifyaccount text txtpassword text messagebox show invalid user password private bool username password getuserpassword username var hasher new hasher num bool hasher password better store compare using else value like bool validate text txtpassword text validate something referring net referring question programming language happens used net example