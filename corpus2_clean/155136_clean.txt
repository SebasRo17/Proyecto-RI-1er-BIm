working wordpress environment heavily relies globals codebase maintaining introduces want change trying think handle globals introduced think set dependency constructor getter setter rely globals refactor old codebase little little globals wordpress globals thinking wrap wp globals inside wrapper hide like public static global would help idea centralize globals one scatter wordpress kill one need modify one place would