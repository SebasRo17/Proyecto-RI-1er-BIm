many application include static really change lifetime application example might list sale area likely fixed list foreseeable future uncommon find static database table often want refer foreign key table simple example table id use primary key description example table least column column might want treat row table example might want set default sale area screen provide different figure area restrict user area best way refer static num hard description use look database need num hard id use look need num add column table purpose e g column could lot num something else special consideration make dealing static database example giving table special