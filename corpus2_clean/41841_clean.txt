currently looking net component fulfills following requirement tiff file extracting single page image writing image new support colored image improving tiff file must etc optional royalty free deployment server fully architecture independent one assembly x64 x86 found net capable handling tiff file api still complex cannot improve file improvement trying aforge net filter require converting image specific pixel format depending operation