question come writing simple text adventure game character inventory include container also thing want clean way style inventory like carrying bag containing ring backpack containing bread currently include formatting result returned query one shot example result recursively check inventory adding multiple tab depending nesting level result would like cleaner separation content presentation query would structure separate pas could style styling pas handle n level deep structure example would styling tool like cs handle need procedurally generate cs structure