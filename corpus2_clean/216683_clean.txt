recently told using enum public enum completed running may compatibility serialization issue thus sometimes better use const public const task end state end state public const task end state task end state public const task end state complete task end state complete public const task end state running task end state running find practical use case may happen guideline enum avoided edit production environment multiple service different version product later version may may include new property task end state example try deserialize new enum older version specific service may work