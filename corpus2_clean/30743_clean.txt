set coordinate want make cluster every point within distance unit neighbor ex point coordinate quickest way mathematica large data set tried find point within mentioned distance table select list euclideandistance list length list trouble join set common element