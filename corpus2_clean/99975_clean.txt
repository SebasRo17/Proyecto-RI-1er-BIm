c c guess language allow taking reference element something similar like accessing individual element done directly via temporary reference pointer example like num n thearray frobnicate thearray num thearray like num n elem thearray elem frobnicate elem num elem obviously body becomes complex refactoring used whole access moved separate would naturally operate single element case used one may convenient keep directly debating form prefer team opinion mixed therefore interested whether exist guideline especially wrt c edit purely style performance irrelevant c actually generate exactly version