got nice fairly popular javascript library following semantic versioning current library dependency library available either separate downloads part single bundled download see need head path want extract additional smaller library one larger library extracted library would available separate file inside one bundled build go path extracting library providing bundled version final require full version change semantic versioning would bump num x num x first thought change public api change major version number wonder well restructuring lot thing even though final api bundled version would clear answer semver something like need bump first second third dot something else