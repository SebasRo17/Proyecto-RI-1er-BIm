let say hierarchy item class rectangle circle triangle want able draw first possibility add virtual draw item public virtual item virtual draw num however want split drawing functionality separate draw library core library contains basic representation couple possibility think num take list item use dynamic cast work draw itemlist item foreach item item item dynamic cast rectangle item drawrectangle else dynamic cast circle item ideal relies rtti force one aware item hierarchy num approach defer drawing responsibility hierarchy etc item virtual drawer num rectangle public item public virtual drawer new achieves separation concern base representation item drawing problem though item class dependent drawing class separate drawing separate library solution item factory description however defined core library dependent draw library