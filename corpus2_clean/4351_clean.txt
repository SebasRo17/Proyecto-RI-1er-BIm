consider following example f x x g x f x traceprint g g g f f time f plus far good however like instruct traceprint treat evaluation f atomic print intermediate step evaluating f next line f would without excluding line first idea make f read protected setattributes f readprotected traceprint g g g f f time f plus see inner working f still traced however attribute otherwise honoured f global f attribute f readprotected another thing work traceprint g except f give complete trace instruct traceprint treat function f atomic