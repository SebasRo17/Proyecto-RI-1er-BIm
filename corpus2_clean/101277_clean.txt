studying structure right still need find teacher site book clear explanation macro aspect topic mean lesson text book mix everything together logical structure start linked list talk stack queue heap priority far tell linked list stack really category use linked list implement different type e g stack queue etc would right say type structure stack queue hash table heap tree array static linked list dynamic tool going use implement structure statement completely correct maybe linked list indeed structure therefore would using one structure e g linked list build structure top e g stack