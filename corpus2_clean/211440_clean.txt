related extends evil v ocp separate idea implement interface exist writing pull webpage going need hold cookie authentication going use request session form never sure approach appropriate choice request session def init self super init def init self self session request session former seems end way public method need latter look like might unnecessarily convoluted since pretty much every going involve call session plus maybe parsing json find