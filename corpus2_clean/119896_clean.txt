defines private well container exact since container using information within said container part logic ability add delete element said container unit test need populate container depends test done thus putting setup would impractical bloated plus could add unwanted side effect since private add thing via public interface run need unit test case copy paste another test currently mocking whole container somehow feel elegant solution due mocking frame work mock requires container public use patch dict would rather keep private pattern one use still populate container without public test way mock patch dict missed