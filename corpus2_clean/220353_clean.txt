play around emulating simple old cpu set structure far anyway follows device creates instance cpu cpu creates instance register clock mmu position mmu need access content register figure could probably bodge something together passing entire register mmu called seems unwieldy thing thought would initialise mmu register constructor mistaken still reference meaning change register mmu affect whole system