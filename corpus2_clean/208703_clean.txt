recently came across name french could use google translate part need translated would practical copy paste variable translator easily translate name translator could use french tried translate include stdio h unsigned unsigned unsigned num num num num num num printf n tried translate using google translate got bad translation include stdio h unsigned number unsigned divisor unsigned left count num number num number divisor num left num divisor number rest num divider remainder number divisor left num printf n count