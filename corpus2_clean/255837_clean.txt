struggle every time work project entirely sure standard convention creating view id usually use something like view button let say multiple dialog view button start view button dialog1 spiral downward suggestion