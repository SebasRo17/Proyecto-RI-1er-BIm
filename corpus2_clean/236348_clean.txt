trying make number class c xna game would essentially wrapper around public interface struct point size get x get set example might heightmap essentially float might level byte could entirely separate class specific helper method etc also made small extension library operates specifically num dimensional public static transformation static transpose static static static static however actually implement encounter small design issue backing would private protected client actually us would unable use extension library according ca1819 eric blog publicly exposing even via property bad idea would fully mutable suggested workaround clone expose immutable mean still use library overthinking thing elegant solution really like idea using indexer read write situation might want work entire