draw object specified color objects varying colors erase without redraw everything else whats happening draw using gxcopy drawn specified color erase using gxcopy foreground background reversed erases whatever overlayed need redraw items crossed yuch draw using gxinvert drawn random color nothing resembling requested properly restores color underlying objects erase mode whats solution