I know that if I use               CenterDot @@ (Superscript @@@ FactorInteger[x])      _Mathematica_ will pretty print the output. However, is it possible to tell _Mathematica_ to automatically format the output of `FactorInteger` with `CenterDot` and `Superscript`?