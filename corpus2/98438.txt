This happens in Javascript, for example, that division by zero is not handled by the language itself. Is there's a valid reason for this? I see this as a very basic exception that a language should be able to handle (they even handle 0/0 as NaN NaN). > or take the square root of a negative number or use arithmetic operators > with non-numeric operands that cannot be converted to numbers.