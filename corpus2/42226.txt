I need to execute this function thousands of times, and the faster, the better. I came up with two versions, but wanted to see if you can come up with an even faster way. Is there a better way? I use the list:               symbolsList={r,A,b}      And the function checks if my expression contains all those symbols:               expression=r^2+A*b-3      First version:               ContainsAllSymbols[expression_]:=Block[{containsAllSymbols},         containsAllSymbols=True;         Do[containsAllSymbols=containsAllSymbols&&!FreeQ[expression,symbolsList[[dvi]]];If[!containsAllSymbols,Break[]],{dvi,Length[symbolsList]}];         containsAllSymbols     ]      Second version (faster):               ContainsAllSymbols[expression_]:=Scan[If[FreeQ[expression,#],Return[False]]&,symbolsList]=!=False