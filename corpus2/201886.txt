I read that a computer is made up of the CPU and the memory. I also read that the CPU itself contains a cache which is memory. So if the CPU itself contains the memory, isn't the CPU a computer? Please clarify, thanks. EDIT: I read here: http://www.dwightsilverman.com/future.htm it says: > A processor is, itself, a computer, and they now are small enough that they > are embedded in everything from car engines to toasters. Although these > processors are relatively dumb, designed to do just one thing, smarter > processors that are more versatile also are finding their way into tiny > places.