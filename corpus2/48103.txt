I am wondering is there a way to use loop, like `Do`, `For`, to rewrite the last line in the code. I tried to use `Do` to make this work, but mathematica keeps telling me that "svars will be suppressed during this calculation". I want to do the loop way, not the `Table` way i used below, because `Tabel` gives me a really ugly format of eigenvector. ![enter image description here](http://i.stack.imgur.com/S1rv2.jpg) ![enter image description here](http://i.stack.imgur.com/Lygo1.jpg) ![enter image description here](http://i.stack.imgur.com/nu0LW.jpg)               Remove["Global`*"]          Nmax = 4;          T = 1/2 Sum[m[i] x[i]'[t]^2, {i, 1, Nmax}];     U = 1/2 Sum[k[i] (x[i][t] - x[i - 1][t])^2, {i, 1, Nmax + 1}];     L = T - U;          EL[q_] := D[L, q] - D[D[L, D[q, t]], t]          eigen = Table[EL[x[i][t]], {i, 1, Nmax}];          x[i_][t_] = a[i] E^(I \[Omega] t);          sim = eigen /. t -> 0;     x[0][t] = 0;     x[Nmax + 1][t] = 0;          For[i = 1, i <= Nmax + 1, i++, m[i] = m; k[i] = k; m[5] = 0]          matrix = D[sim, {Array[a, Nmax]}];          Print["\nThe reduced eigenmatrix looks like ", matrix // MatrixForm]          soln = \[Omega] /. Solve[Det[matrix] == 0, \[Omega]];          eigenval = Pick[soln, Resolve[ForAll[{k, m}, k > 0 && m > 0, Positive[#]]] & /@ soln];          Do[Print["\n\nThe positive eigenfrequencies we computed are " \[Omega][i] , " = ", eigenval[[i]]], {i, 4}]          b = Table[a[i], {i, Nmax}];          Table[Solve[matrix.b == 0 /. \[Omega] -> eigenval[[i]], b], {i, 1,Nmax}]