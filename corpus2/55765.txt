Thanks for your attention. I'm new to mathematica. I have the codes as below. But the If statement does not work. Anything wrong with my code? Thanks for help! Ben               ClearAll;     (*Define of hill function*)          Hillinh[x_, nx_, x0_] = 1./(1. + (x/x0)^nx) ;     Hillexc[x_, nx_, x0_] = 1. - Hillinh[x, nx, x0];     Hillshift[x_, nx_, x0_, lamda_] = lamda + (1. - lamda)*Hillinh[x, nx, x0];     (*Parameters*)     aa =  2(*gx/(k*xT)*);     (*bb= 10 gy/(k*yT)*)     py = 4;     px = 4;     yT = 1;     xT = 1;     data = {};          For[i = 1, i <= 20, i++,           bb = i*0.5;           (*Equations for system*)      eqs1 = aa*Hillinh[y, py, yT] - x;      eqs2 = bb*Hillexc[x, px, xT] - y;      eqscomb = {eqs1, eqs2};           (*calculate the fixpoint and eigenvalues*)      r = {x, y};       fp = NSolve[Rationalize[Thread[eqscomb == 0 && x > 0 && y > 0]], r, Reals];           (*Calculate tau*)      xs = x /. fp;      ys = y /. fp;           alpha = (py/aa)*xs^2*ys^(py - 1);      beta = (px)*(ys/xs)*(1 - ys/bb);      test = alpha*beta - 1;                If[test <= 0, Continue[], Print[-2*ArcTan[Sqrt[test]]/Sqrt[test]+Pi/Sqrt[test]]];           ]