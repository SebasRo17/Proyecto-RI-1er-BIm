when working with 2D, RegionPlot works fine with ParametricRegion as can be seen in this example:               r = ParametricRegion[{Cos[t], Sin[t]}, {{t, 0, 2 Pi}}];     RegionPlot[r]      however, when working with 3D ParametricRegion, the RegionPlot3D run forever. check this example:               r2 = ParametricRegion[{Cos[u], Sin[u] + Cos[v],          Sin[v]}, {{u, 0, 2 Pi}, {v, -Pi, Pi}}];         RegionPlot3D[r2]      nothing in the documentation said about functions ParametricRegion and RegionPlot3D that they should not be use for 3D parametric Region. any explanation why RegionPlot3D run non stop when plotting r2? MMA 10 on windows 8. thanks