I want to define a function, `PolynomialInterpolation` to find the coefficients of a polynomial interpolation function. My trial is as shown below:               PolynomialInterpolation[tb_, tf_, θb_, θf_, θbDot1_, θfDot1_, θbDot2_, θfDot2_] :=        Module[         {InterpolationResult, u, θ, θDot1, θDot2, c0, c1, c2, c3, c4, c5, PolynomialParameter},         θ[t_] := c0 + c1 t + c2 t^2 + c3 t^3 + c4 t^4 + c5 t^5;         θDot1[t_] := D[θ[u], u] /. u -> t;         θDot2[t_] := D[θ[u], {u, 2}] /. u -> t;         PolynomialParameter =            Solve[{             θ[tb] == θb,              θ[tf] == θf,              θDot1[tb] == θbDot1,              θDot1[tf] == θfDot1,             θDot2[tb] == θbDot2,              θDot2[tf] == θfDot2             },             {c0, c1, c2, c3, c4, c5}]       ]      However, when I use my function `PolynomialInterpolatio`, it returns:               PolynomialInterpolation[0, 5, 30, 75, 0, 0, 5, - 5]      > >     {{c0$698 -> 30, c1$698 -> 0, c2$698 -> 5/2, c3$698 -> 8/5, >       c4$698 -> -(29/50), c5$698 -> 29/625}} >   So my question is why does the result contain variables with `$` in their names?