I'm curious as to how other people indent/break long jQuery chains, as I can never decide what is more readable (particularly when using .end() to "close" a block of methods e.g.               $(this).not(":has(.pointer)").append(pointerHtml)                     .end()                 .closest("li").toggleClass("selected")                 .siblings().removeClass("selected")                 .andSelf().removeClass("pre-selected").animate({                                      left: 12                                      opacity: 0.5                                 })                     .end().end()                 .filter(".selected").prev().addClass("pre-selected")                                 .end().children().load("/homepage.html", function() {                                            //code                                  }).end().end().addClass("processed");      How would YOU format this? *edit included a method with a callback to make it a bit more interesting