I am bothered by the output format of the last two elements of `efre`. Is there a way to make these two look like the same format as the first two in `efre`?               Remove["Global`*"]          coe = k/m SparseArray[{{i_, i_} -> -2, {i_, j_} /; Abs[i - j] == 1 ->            1} , {4, 4}];     efre = Sqrt[-coe // Eigenvalues] // Simplify      evec = coe // Eigenvectors // Simplify;     Column[Subscript[ω, #] & /@ Range@4 == efre // Thread,       Spacings -> 2]      ![enter image description here](http://i.stack.imgur.com/YcBoX.jpg)