The normal-gamma mixture is:               NIntegrate[      Evaluate[(Sqrt[a]*sd)^(-1)*        PDF[NormalDistribution[0, 1], (x - mu)/(Sqrt[a]*sd)]*        PDF[GammaDistribution[kr, kr^(-1)], a]], {a, 0, \[Infinity]}]      where the three variables are: mu - mean; sd - standard deviation; and kr - kurtosis. The mixture allows defining a "normal" distribution with kurtosis value greater than 3. Instead of the clunky approach of defining the pdf as `func[mu_, sd_, kr_, x_]`, what is the correct syntax to use `ProbabilityDistribution`? The application in mind is to generate pdf tables for different kurtosis values.