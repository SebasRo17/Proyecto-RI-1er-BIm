I am trying to implement a 1D model of coupled field equations where I split up space into pieces to get a system of coupled ODEs which I call Eeqns and Ieqns. The problem I can't figure out is how to put Dirichlet boundary conditions (NE=0, NI=0 at both edges) into the code. Everything I try results in an error. Below is what I've written (which works) without BCs. Pardon the text conversions problems. I don't know how to fix it. Also, f and g are functions defined in the code.               Eeqns = Table[             {10*D[NE[i][t], t] ==             -NE[i][t]+(1-NE[i][t])*             f[α*10*Sum[dx*g[dx*i-dx*j, bEE, σEE]*NE[j][t], {j, 1, n}]             - α*10*Sum[dx*g[dx*i-dx*j, bIE, σIE]*NI[j][t], {j, 1, n}]             + α*10*(P/2)*(Tanh[10*(i-Elefti)]-Tanh[10*(i-Erighti)])*(1/2)*(Tanh[10*(t-Einputstart)]-Tanh[10*(t-Einputstop)]), aE, θE]}, {i, 1, n}];          Ieqns = Table[             {10*D[NI[i][t], t] ==             -NI[i][t]+(1-NI[i][t])*             f[α*10*Sum[dx*g[dx*i-dx*j, bEI, σEI]*NE[j][t], {j, 1, n}]             - α*10*Sum[dx*g[dx*i-dx*j, bII, σII]*NI[j][t], {j, 1, n}]             + α*10*(Q/2)*(Tanh[10*(i-Ilefti)]-Tanh[10*(i-Irighti)])*(1/2)*(Tanh[10*(t-Iinputstart)]-Tanh[10*(t-Iinputstop)]), aI, θI]}, {i, 1, n}];          Einit = Table[NE[i][0] == 0, {i, 1, n}];     Iinit = Table[NI[i][0] == 0, {i, 1, n}];          u = Table[NE[i], {i, 1, n}];     v = Table[NI[i], {i, 1, n}];          sol = NDSolve[           Join[Eeqns, Ieqns, Einit, Iinit],           Join[u, v],           {t, 0, T},           MaxStepSize -> 0.01][[1]];