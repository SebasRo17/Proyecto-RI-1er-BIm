I want to compute the drag of a plate ![enter image description here](http://i.stack.imgur.com/kcEVv.jpg) Below is the code I am using to integrate the function               velcenter = {dotx, doty, 0};                           r = {Cos[θ[t]], Sin[θ[t]], 0};     veltanA = {-Sin[θ[t]], Cos[θ[t]], 0};      velxpoint = Part[Cross[velcenter, r], 3];      dragD = 1/2*ρ*cofD*h*Integrate[(Abs[velxpoint - s*dotθ])*(velcenter +                                             s*dotθ*veltanA), {s, a - l, a}];      The integration is so slow and I want to speed it up. Any suggestions are welcome !