How would you, given a list `{1, 2, 3, 4}`, apply a function `f` to `1` and `2`, then `2` and `3`, etc.?               {f[1,2], f[2,3], f[3,4]}      More generally, how do you define which parts of a list you want to pass/`Map`/`Apply` to a function that takes multiple arguments?