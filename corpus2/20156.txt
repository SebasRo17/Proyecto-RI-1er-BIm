I need to use the dimension of a certain array, to use it as a bounder in a loop afterwards. The problem is that I get a list, when write :               Dimensions[ai]= {2}          aiod = Table[ai[[i]] - 1, {i, 1, Dimensions[ai], 1}]      and it does not work. I need to get "2" instead of {2}.