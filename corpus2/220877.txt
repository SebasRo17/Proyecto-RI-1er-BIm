var Id = from value in Enumerable.Range(0, 100)                                   select value;                  IEnumerable<int> ids = from value in Enumerable.Range(0, 100)                                   select value;                            foreach (var v in Id)             {                 Console.WriteLine(v.ToString());             }                  foreach (var x in ids)             {                 Console.WriteLine(x.ToString());             }      Both will do the same job, but what is the difference?