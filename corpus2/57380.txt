The problem when you copy text from one InputFiled to another. Inserts in unnecessary places gap. For example copy the text "#bbb" (without the quotes) from one field to another get "# bbb" (without the quotes).