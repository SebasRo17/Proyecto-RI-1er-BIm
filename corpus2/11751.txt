`WolframAlpha` is a built-in symbol in MMA8, yet expressions with `WolframAlpha` and the equal sign operator remains unevaluated despite the internet connection.               WolframAlpha["2+1"]          (* WolframAlpha["2+1"] *)           = heart disease risk 50yo male          (* AlphaIntegration`ExtrusionEvaluate["heart disease risk 50yo male", TextForm] *)           ?AlphaIntegration          (* Information::notfound: Symbol AlphaIntegration not found. >> *)      ?