Here's a scenario: whenever accessing class using namespaces in php, it doesn't launch the standard constructor, but only the 'magic' method. What's wrong here and what can be fixed? **hierarchy**                test.php      a      +-foo.php      **test.php**               <?php          spl_autoload_register();     spl_autoload_extensions(".php");          new a\Foo();      **foo.php**               namespace a;          class Foo{      /* //if this ges added the __construct() is called         public function __construct(){             echo "called magic constructor";         }       */              public function Foo(){              echo "called standard constructor";          }     }      Launching test.php doesn't lounches the Foo constructor method, but only the __construct method if present.