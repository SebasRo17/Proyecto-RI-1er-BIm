If I try to start a Mathematica script in the background from the command line, like this:               math -script someFile.m &      then the process gets automatically suspended. Here's a terminal transcript for clarifications: ![](http://i.stack.imgur.com/TvE9z.png) This happens both on Linux and OS X. Other programs don't typically stop when run in the background.   * Why does this happen?   * What can I do to prevent this?