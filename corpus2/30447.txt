Is there a keyboard shortcut for creating something like the result of this:               Quantity["Kilograms" * ("Meters")^2 / ("Seconds")^2]      After copying the result to a `TraditionalForm` field it looks like this (line 3): ![enter image description here](http://i.stack.imgur.com/mcvNo.png) Is there a shortcut for creating something like this in `TradionalForm` without having to type `Quantity[..., ...]` directly?