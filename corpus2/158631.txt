> **Possible Duplicate:**   >  When are Getters and Setters Justified Why do we use get and set method in C#? And why do we use public and private method property? For example:               public class Date     {         private int month = 7;           public int Month         {             get             {                 return month;             }             set             {                 if ((value > 0) && (value < 13))                 {                     month = value;                 }             }         }     }