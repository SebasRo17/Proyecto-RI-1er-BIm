I have a query regarding big endian and little endian. Basically the conversion is used to `reverse the byte order in memory` . When we need to do the conversion, do we need convert each and every data types?