Given an expresion 'e' in terms of a function 'f' and given another function 'g', how could I write a Module that replaces the function f to g in the expresion e and returns it? An example:               MyFunction[expresion_, f_, g_] := (...)      `MyFunction[f[x]-3, f, Sin[x]]` should evaluate to `Sin[x]-3` `MyFunction[D[f[x],x], f, x]` should evaluate to `1` `MyFunction[f[x], f, 19]` should evaluate to `19` I hope it's clear. Sorry about my bad english.