I have a lot of variables: $a,b,c,d, \ldots $ I want to clear the values of all of them except $b$. It's really lengthy to write               Clear[a];      Clear[c];     ...      Is there any command which clears all variables except a chosen one?