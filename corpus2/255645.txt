I always wondered, why doesn't this compile?               for(int i=0, int q=0; i<a.length; i++){        ...     }      Why doesn't Java allow me to declare more than one variable in the for loop 'header'? Is there a logical reason behind this?