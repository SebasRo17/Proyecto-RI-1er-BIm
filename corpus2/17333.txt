I've seen a notebook with the following:               FullSimplify[      Solve[{eq1, eq2, eq3, eq4, eq5, eq6, eq7, eq8, eq9, eq10, eq11, eq12},            {hbbbf, hbcf, hbbcr, hbcc, hbvbf, hbbc, hsbbf, hssd, hsvbf, hsf, hsbr, hsc}      ]     ][[1]] // N      I searched the _Mathemtatica_ help documentation for `//` and it seems to be a postfix operator. On the other hand `N` seems to be a way to specify the precision of a numerical value. What does the `[[1]] // N` mean? Thanks.