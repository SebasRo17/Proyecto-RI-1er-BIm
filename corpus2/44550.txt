Let $a\in\mathbb{N}^n$ and $k\in\mathbb{N}\!=\!\{0,1,2,\ldots\}$. How can I efficiently generate the list $$\{(a_1,\ldots,a_k); a_1,\ldots,a_k\!\in\!\mathbb{N}^n\!\setminus\!\{0\}, a_1\!+\!\ldots\!+\!a_k\!=\!a\}?$$ It would also be desirable for the code to be short. My current, highly inefficient solution is:               Select[Tuples[Tuples[Range[0, Max @@ a], Length@a], k],        Plus @@ # == a && !MemberQ[#, Table[0, {i, n}]]&]