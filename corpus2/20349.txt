I would like to style each element of my list using `Style`. But when I try               Lista = {{"a", "b", "a"}, {"n", "g", "r"}, {"d", "fgh", "Df"}}     Dimensions[Lista]     FormattedLista = Style[Lista, FontSize -> 10]     Dimensions[FormattedLista]      the structure of the original list is lost. How can I map `Style` onto elements at the last level only, i.e. without changing the structure of the list?