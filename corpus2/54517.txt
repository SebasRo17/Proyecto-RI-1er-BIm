Working some charts on V10, I discovered that `PlotLegends` disappears if used together with `ColorFunction` inside `ListPlot` or `Plot`. Here is one example for `Plot`:               Plot[Sinc[x],{x,0,10},PlotLegends->{"Teste"},PlotStyle->Thick]      ![enter image description here](http://i.stack.imgur.com/PVv1U.png)               Plot[Sinc[x],{x,0,10}, PlotLegends->{"Teste"}, PlotStyle->Thick          ,ColorFunction->(ColorData["AvocadoColors"][#2]&)     ]      ![enter image description here](http://i.stack.imgur.com/pPxzt.png) The same problem to ListPlot. I'm on Mac using V10. Anyone else confirm this?