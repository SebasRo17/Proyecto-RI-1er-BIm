I would like to use a different norm instead of the 2-norm in `FindFit` ( _Mathematica_ 9). For example, instead of using $$\sqrt{\sum (x_{\mathrm{model}} - x_{\mathrm{data}})^2}$$ I'd like to use $$\sum \frac{x_{\mathrm{model}}}{x_{\mathrm{data}}}$$ I can tell `FindFit` to use a user-created function as the `NormFunction`. However, I am unsure of the syntax to use when defining $x_{\mathrm{model}}$. Can anyone point me to example code for using a user-defined `NormFunction` in `FindFit` or `NDSolve`?