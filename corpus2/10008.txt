> **Possible Duplicate:**   >  Difference in Plot when using Evaluate vs when not using Evaluate   >  Why do I have to put Evaluate[] here   >  Why is ContourPlot not displaying this curve? Why is nothing (empty plot, without error) plotted, when using the following code?                tab = Table[x^2 + y^2 == R, {R, 0.2, 1, 0.2}];      ContourPlot[tab, {x, -1, 1}, {y, -1, 1}]      Thank you for your help!