Working on two formats throughout our website - PC and mobile. Each request will have a check who the client is. Then, two methods seem optional:   1. Render the page accordingly.   2. Redirect to a parallel site Is there a difference in server load (i.e. maybe 2 has less load because requests are actually pointed at different pages)? Whereas, it seems having a single page would be easier to maintain.