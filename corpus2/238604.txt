For SQL and C# app dev jobs, the interviewers usually ask questions about tree,graph and linked list traversals using pure C and pointers. In the 3 years I've spent in my job, I've never had to actually > find the path to the 1st node to the right of a given node which is a > multiple of the given node for example I can see that these skills might be used in jobs where you need to write compilers, drivers and work on the OS kernel. Other than those, where else are these skills used?