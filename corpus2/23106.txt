Assume that;               w[n_] :=Expand[Sum[Binomial[n - k - 1, k]*(-1)^k*A^(n - 2*k - 1), {k, 0, n - 1}]]     f[x_,y_, z_]:=PolynomialRemainder[(w[z] - 1)*(w[y] - 1), (w[x] - 1), A]      According to the above definitions, the function `f` depends on `x,y,z` and `A`. I want to have a new function only depends on `A` for any `x,y,z`. For example, when i put some values for `x,y,z`, i want a function such `g[A_]:=.........`How can i obtain? Thanks...