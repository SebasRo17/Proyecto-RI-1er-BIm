Why does the following code produce different result? In my mental model, they should be the same.               Table[With[{x = i^k}, HoldForm[x]], {k, 1, 5}]     With[{x = i^k}, HoldForm[x]] // Table[#, {k, 1, 5}] &      Output:               {i,i^2,i^3,i^4,i^5}     {i^k,i^k,i^k,i^k,i^k}