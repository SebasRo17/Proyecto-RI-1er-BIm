I am trying to create a spreadsheet that pulls the following information for each of the users in our domain:   * Name   * Email Address   * Mobile Phone Number (Saved in "Mobile")   * Department   * Title   * Extension Number (saved in "Work")   * Google+ Profile Image URL I'm pretty new at scripting and JavaScript so I'm having some trouble with the special, specific fields. I'm using this Google Reference page for help, but I need help with the phone numbers and organizations information. I can pull `user.phones` and `user.organizations`, but all the data that comes together is messy. I also know that I can get the Google+ Profile Image URL by using `profile.image.url` if I define a variable for `PlusDomains`.               function listAllUsers() {         // set spreadsheet       var ss = SpreadsheetApp.getActiveSpreadsheet();            // set sheet       var sheet = ss.getSheets()[0];            // set range       var range = sheet.getRange(3, 1);            // clear range       range.clear();               var pageToken, page;       do {            page = AdminDirectory.Users.list({       domain: 'test.org',       orderBy: 'givenName',       maxResults: 500,       pageToken: pageToken       });      var users = page.users, auserInfo = new Array();           if (users) {       for (var i=0, len=users.length; i<len; i++) {         var user = users[i];         Logger.log('%s (%s)', user.name.fullName, user.primaryEmail);         auserInfo.push([user.name.fullName, user.primaryEmail, user.phones, user.organizations]);       }       } else {       Logger.log('No users found.');       }      pageToken = page.nextPageToken;      } while (pageToken);            // set values to sheet       sheet.getRange(3,1,len,4).setValues(auserInfo);            }