How can we create a Vlookup function, similar to what we have in Excel? So if we have a table `a` and another `b`, we could add columns to table `a`, based on a common key to both, in order to create a table `c`. ![Tables](http://i.stack.imgur.com/89Bad.png)