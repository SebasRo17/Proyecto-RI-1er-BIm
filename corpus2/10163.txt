What I am doing wrong? I would like to get a header to the table and based on your code I tried this:               hdr = {{"Calculated Coordinates", SpanFromLeft}, {"No", "x", "y"}};     hdr1 = Grid[hdr, Frame -> All];          legendmarkers=Range@Length@data;                          legendlabels=Map[Style[AccountingForm[#, 10], FontSize -> 20, FontFamily -> "Calibri"] &, data, {-1}];                          styledheaders=Style[#,Bold,FontSize -> 22,FontFamily -> "Calibri"] & /@ {hdr1};                          legendGrd = Grid[Prepend[Transpose[Join[{legendmarkers},      Transpose[legendlabels]]], styledheaders], Frame -> All,     BaseStyle -> {FontSize -> 20, FontFamily -> "Calibri"},     FrameStyle -> GrayLevel[.9], Alignment -> {Center, Center},     Background -> {None, {GrayLevel[.5], {GrayLevel[.8]}}}]      The result is: ![Legend](http://i.stack.imgur.com/woyJl.jpg) "Calculated Coordinates" should span all three columns, and "No", "x", and "y" should be the header of the corresponding columns. What must I do to get this? Again, thanks a lot!!