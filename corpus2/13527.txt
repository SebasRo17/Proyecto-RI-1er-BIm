The `DensityHistogram` command lets you specify a `ChartElementFunction` which according to the documentation: > The arguments supplied to `ChartElementFunction` are the bin region > `{{xmin,xmax},{ymin,ymax}}`, the bin values _lists_ , and metadata > `{m1,m2,...}`. My question is how can I use the bin values lists and the metadata? When I try to access them all I get something like               System`DensityHistogramDump`thisBinList$270262