Please correct the method "ave".               public class methods{         public static void main (String[] args){         int[] a = {1,2,3,4,5,6,7,8};         System.out.println("Sum is: " + sum(a));         System.out.println("Average is: " + ave(a));         System.out.println("Minimum is: " + min(a));         System.out.println("Maximum is: " + max(a));         System.out.println("Minimum appears: " + countmin(a) + " times");         System.out.println("Maximum appears: " + countmax(a) + " times");     }     //sum of the arrays     public static int sum(int[] array){         int sum = 0;         for (int i = 0; i < array.length; i++){             sum += array[i];         }         return sum;     }     //average     public static int ave(int[] array){             double ave;             for (int i = 0; i < array.length; i++){                 ave = sum(array)/array.length;             }             return ave;     }     //displays the smallest number     public static int min(int[] array){         int min = array[0];         for (int i = 0; i < array.length; i++){             if (array[i] < min)                 min = array[i];         }         return min;     }     //displays the biggest number     public static int max(int[] array){         int max = array[0];         for(int i = 0;i < array.length; i++){             if (array[i] > max)                 max = array[i];         }         return max;     }     //displays how many times the min appeared     public static int countmin(int[] array){         int countmin = array[0];         for(int i = 1;i < array.length; i++){             if (array[i] == min(array))                 countmin++;         }         return countmin;     }     //displays how many times the max appeared     public static int countmax(int[] array){         int countmax = 0;         for (int i = 1; i < array.length; i++){             if (array[i] == max(array))                 countmax++;         }         return countmax;     }     }