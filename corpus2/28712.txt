I am trying to evaluate the expression               Expectation[(B x (-6 b + 12 a n0 + B x + B^2 x^2))/(2 (6 a + B x + B^2 x^2)),        Distributed[x, BetaDistribution[α, β]]]      The evaluation runs for a few seconds and then _Mathematica_ displays the unevaluated expression without any error messages or any other output. Is this because this random variable transformation is too complex for _Mathematica_ to handle? If so, shouldn't some message be generated? ### Edit The same expression with an `UniformDistribution`               Expectation[(B x (-6 b + 12 a n0 + B x + B^2 x^2))/(2 (6 a + B x + B^2 x^2)),        Distributed[x, UniformDistribution[{α, β}]]]      completes and returns a formula.