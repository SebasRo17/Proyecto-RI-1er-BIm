Related question: How can I convert a complex number a+b I to the exponent form A Exp(I phi)? _Mathematica_ insists on displaying complex number in form `a+I b` when `a` or `b` are not exact:               Clear["Global`*"]     z = 3 + 4 I;     Abs[z] Exp[I Arg[z]]      ![Mathematica graphics](http://i.stack.imgur.com/bDDfo.png)               z = 3.0 + 4 I;     Abs[z] Exp[I Arg[z]]      ![Mathematica graphics](http://i.stack.imgur.com/bbKpq.png) What I'd like is a `polarForm` wrapper that keeps the polar form even when `a` or `b` are not exact, like this:               Clear["Global`*"]     (z = 3.0 + 4 I) // polarForm      ![Mathematica graphics](http://i.stack.imgur.com/VSOB1.png)               (z = 3 + 4 I) // polarForm      ![Mathematica graphics](http://i.stack.imgur.com/LT0Dp.png) In the above, `polarForm` is the wrapper needed.