I would like to make a "Stacked" `BarChart` where the bars have variable widths, like a `RectangleChart`. However, `RectangleChart` does not accept `ChartLayout -> "Stacked"`. Is there a way to do this, or does anyone have a workaround? For example, here is a `RectangleChart` with the bars at different widths: ![enter image description here](http://i.stack.imgur.com/9001Q.png) Here is a "Stacked" `BarChart` with the bars decomposed into two components: ![enter image description here](http://i.stack.imgur.com/VW9C6.png) I would like to make a stacked `RectangleChart` with variable-width bars that can be decomposed into components.