I need to compare solutions in random systems with the same noise and different initial conditions. Here I run twice a random map with the same initial condition, inside a `BlockRandom`. The output expected should be exactly the same orbit but they are different. What have I done wrong?               map[x_, α_] := α/2 ArcTan[x] + 1/2 x          ClearAll[α]     noisymap[x_, α_, σ_] := map[x, α] + σ RandomReal[{-1, 1}]          ClearAll[noisy1, noisy2]     BlockRandom[         noisy1 = NestList[With [{α = 2, σ = 0.1}, noisymap[#, α, σ] &], -0.32, 1000];         noisy2 = NestList[With [{α = 2, σ = 0.1}, noisymap[#, α, σ] &], -0.32, 1000];     ]          ListLinePlot[{noisy1, noisy2}]          ListLinePlot[Abs[noisy1 - noisy2]]