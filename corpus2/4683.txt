Do[       PutAppend[Table[i, {RandomInteger[100]}], "./file.m"],       {i, 100}     ]      I'd like each inner list to always appear on the same line, but not automatically wrapped at 68th character. How to achieve that? I wonder where and how the PageWidth option should be set for the InputStream used by the PutAppend.               #!/usr/bin/env bash          sed -e '         /.* $/ {             N             /\n .*$/ {                 s/\(.*\) \n \(.*\)/\1 \2/             }         }     ' $1      will fix the broken lines.