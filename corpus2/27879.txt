Let $(x_{min},x_{max})$ and $(y_{min},y_{max})$ represent lowerbound and upperbound acceptable values for some coordinate $c_i$. Setting $x_{min} = y_{min} = 1$ & $x_{max} = y_{max} = 9$, how can I take a list of values, e.g.:               val = {{0, 10}, {2, 10}, {4, 4}, {5, 6}, {10, 4}};      and use `DeleteCases` to get rid of coordinates such as `{0, 10}, {2,10}, & {10,4}` (the examples in the above list) where _either_ the $x$ or $y$ component of a pair is outside of the specified upper and lower bounds for acceptable values? Is `DeleteCases` an appropriate tool for large arrays?